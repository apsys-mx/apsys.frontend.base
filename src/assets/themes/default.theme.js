import { createTheme } from '@mui/material/styles'

/**
 * Default theme
 */
const defaultTheme = {
	palette: {},
	typography: {
		fontFamily: 'Roboto',
	},
}

export default createTheme(defaultTheme)
